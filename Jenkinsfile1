
pipeline {
    agent { label 'sonar-node' }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn clean'
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
