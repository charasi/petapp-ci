
pipeline {
  agent { label 'sonar-node' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') { 
            steps {
                sh './mvne clean'
                sh './mvnw package'
            }
        }

    stage('Run') { 
            steps {
                sh 'java -jar target/*.jar'
            }
        }
}
}
