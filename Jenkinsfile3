pipeline {
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('mariah-db') 
  }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i petclinic.hosts --private-key=$ANSIBLE_PRIVATE_KEY sample.yml'
      }
    }
  }
}
